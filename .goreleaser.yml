---
# WARNING: This file is autogenerated - changes will be overwritten if not made via https://github.com/pulumi/ci-mgmt
project_name: pulumi-provider-template
builds:
  - id: provider
    binary: pulumi-resource-provider-template
    ldflags:
      - -X github.com/Cogni-AI-OU/pulumi-provider-template/provider.Version={{.Tag}}
  - id: build-provider-sign-windows
    dir: provider
    env:
      - CGO_ENABLED=0
      - GO111MODULE=on
    goos:
      - windows
    goarch:
      - amd64
      - arm64
    main: ./cmd/pulumi-resource-provider-template/
    ldflags:
      - -X github.com/Cogni-AI-OU/pulumi-provider-template/provider.Version={{.Tag}}
    binary: pulumi-resource-provider-template
    hooks:
      post:
        - make sign-goreleaser-exe-{{ .Arch }}
archives:
  - name_template: '{{ .Binary }}-{{ .Tag }}-{{ .Os }}-{{ .Arch }}'
    id: archive
snapshot:
  name_template: '{{ .Tag }}-SNAPSHOT'
changelog:
  skip: true
release:
  disable: false
blobs:
  - provider: s3
    region: us-west-2
    bucket: get.pulumi.com
    folder: releases/plugins/
    ids:
      - archive
